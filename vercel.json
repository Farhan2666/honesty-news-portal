{
  "installCommand": "npm install --install-strategy=nested",
  "buildCommand": "npx prisma db push --accept-data-loss && next build",
  "crons": [
    {
      "path": "/api/news/sync-cron",
      "schedule": "0 6 * * *"
    }
  ]
}
